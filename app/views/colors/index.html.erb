<%- model_class = Color -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.pluralize.titleize %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th class="text-center"><%= model_class.human_attribute_name(:id) %></th>
      <th class="text-center"><%= model_class.human_attribute_name(:name) %></th>
      <th class="text-center"><%= model_class.human_attribute_name(:hex) %></th>
      <th class="text-center"><%= model_class.human_attribute_name(:created_at) %></th>
      <th class="text-center"><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @colors.each do |color| %>
      <tr>
        <td class="text-center"><%= link_to color.id, color_path(color) %></td>
        <td class="text-center"><%= color.name %></td>
        <td class="text-center"><%= color.hex %></td>
        <td class="text-center"><%= color.created_at.strftime("%b %d, %Y") %></td>
        <td class="text-center">
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_color_path(color), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      color_path(color),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<div class="row">
  <%= link_to t('.new', :default => t("helpers.links.new")),
              new_color_path,
              :class => 'btn btn-primary' %>
</div>
